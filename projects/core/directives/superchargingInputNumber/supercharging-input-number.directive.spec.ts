import { SuperchargingInputNumberDirective } from './supercharging-input-number.directive';

describe('SuperchargingInputNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new SuperchargingInputNumberDirective();
    expect(directive).toBeTruthy();
  });
});
